import React from "react";
import ReactDOM from "react-dom/client";
import InventorySystem from "../app";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InventorySystem />
  </React.StrictMode>
);
